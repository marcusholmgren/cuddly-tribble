[project]
name = "cuddly-tribble"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "comtrade>=0.1.2",
]

[dependency-groups]
dev = [
    "pytest>=8.4.2",
]
